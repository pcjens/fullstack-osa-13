const blogRouter = require('./blogs');
const userRouter = require('./users');
const loginRouter = require('./login');
const authorRouter = require('./authors');

module.exports = { blogRouter, userRouter, loginRouter, authorRouter };
